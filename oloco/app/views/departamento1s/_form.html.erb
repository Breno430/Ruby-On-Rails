<%= form_with(model: departamento1) do |form| %>
  <% if departamento1.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(departamento1.errors.count, "error") %> prohibited this departamento1 from being saved:</h2>

      <ul>
        <% departamento1.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
